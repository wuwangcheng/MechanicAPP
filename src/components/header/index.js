import React, { Component } from 'react';
import { View, Text, } from "react-native";

export default header = () => {
    return (
        <View>
            <Text>Header...</Text>
        </View>
    )
}